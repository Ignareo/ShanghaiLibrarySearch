# Integration Tests

These tests exercise the real application stack against the public Shanghai Library website.

## Scope

- TypeScript service-level live search flow
- TypeScript service-level live holdings flow
- MCP stdio tool and resource discovery
- MCP stdio end-to-end tool execution flow

The tests intentionally avoid mocks for the core retrieval flow.
